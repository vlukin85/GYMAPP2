import { useState } from "react";
import { Pressable, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";
import { router, useLocalSearchParams } from "expo-router";
import { ScreenContainer } from "@/components/screen-container";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { exercises, type ProgramExercise } from "@/lib/workout-data";
import { useWorkoutStore } from "@/lib/workout-store";
import { useColors } from "@/hooks/use-colors";

export default function NewProgramScreen() { const colors = useColors(); const params = useLocalSearchParams<{ exerciseId?: string }>(); const { addProgram } = useWorkoutStore(); const first = params.exerciseId ?? "bench-press"; const [name, setName] = useState("Моя тренировка"); const [selected, setSelected] = useState<string[]>([first, "barbell-row", "squat"]); const [sets, setSets] = useState("3"); const [reps, setReps] = useState("8"); const [weight, setWeight] = useState("40"); const [rest, setRest] = useState("90"); return <ScreenContainer edges={["top", "left", "right", "bottom"]} className="px-5" containerClassName="bg-background"><ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.content}><View style={styles.nav}><Pressable onPress={() => router.back()}><IconSymbol name="chevron.left" size={27} color={colors.foreground} /></Pressable><Text style={[styles.navTitle, { color: colors.foreground }]}>Новая программа</Text><Pressable onPress={() => { const items: ProgramExercise[] = selected.map((id) => ({ exerciseId: id, sets: Number(sets) || 3, reps: Number(reps) || 8, weight: Number(weight) || 0, rest: Number(rest) || 90 })); addProgram({ id: `custom-${Date.now()}`, name, description: `${selected.length} упражнения · собственный план`, exercises: items }); router.replace("/(tabs)/programs"); }}><Text style={[styles.saveTop, { color: colors.primary }]}>Сохранить</Text></Pressable></View><Text style={[styles.title, { color: colors.foreground }]}>Собери свой план</Text><Text style={[styles.subtitle, { color: colors.muted }]}>Программа сохранится на устройстве и будет доступна в календаре.</Text><Text style={[styles.label, { color: colors.muted }]}>НАЗВАНИЕ</Text><TextInput value={name} onChangeText={setName} style={[styles.nameInput, { backgroundColor: colors.surface, color: colors.foreground }]} /><Text style={[styles.section, { color: colors.foreground }]}>Упражнения</Text>{exercises.map((exercise) => { const active = selected.includes(exercise.id); return <Pressable key={exercise.id} onPress={() => setSelected((current) => active ? current.filter((id) => id !== exercise.id) : [...current, exercise.id])} style={[styles.exercise, { backgroundColor: colors.surface, borderColor: active ? colors.primary : colors.border }]}><View style={[styles.check, { backgroundColor: active ? colors.primary : colors.background }]}>{active && <IconSymbol name="checkmark" size={16} color="#101412" />}</View><View style={{ flex: 1 }}><Text style={[styles.exerciseName, { color: colors.foreground }]}>{exercise.name}</Text><Text style={[styles.exerciseGroup, { color: colors.muted }]}>{exercise.group} · {exercise.equipment}</Text></View></Pressable>; })}<Text style={[styles.section, { color: colors.foreground, marginTop: 5 }]}>План по умолчанию</Text><View style={styles.fields}>{([{ label: "Подходы", value: sets, setter: setSets }, { label: "Повторы", value: reps, setter: setReps }, { label: "Вес, кг", value: weight, setter: setWeight }, { label: "Отдых, сек", value: rest, setter: setRest }]).map((field) => <View key={field.label} style={{ flex: 1 }}><Text style={[styles.fieldLabel, { color: colors.muted }]}>{field.label}</Text><TextInput keyboardType="decimal-pad" value={field.value} onChangeText={field.setter} style={[styles.field, { backgroundColor: colors.surface, color: colors.foreground, borderColor: colors.border }]} /></View>)}</View><Pressable onPress={() => { const items: ProgramExercise[] = selected.map((id) => ({ exerciseId: id, sets: Number(sets) || 3, reps: Number(reps) || 8, weight: Number(weight) || 0, rest: Number(rest) || 90 })); addProgram({ id: `custom-${Date.now()}`, name, description: `${selected.length} упражнения · собственный план`, exercises: items }); router.replace("/(tabs)/programs"); }} style={[styles.button, { backgroundColor: colors.primary }]}><Text style={styles.buttonText}>Сохранить программу</Text></Pressable></ScrollView></ScreenContainer>; }
const styles = StyleSheet.create({ content: { paddingTop: 16, paddingBottom: 32, gap: 12 }, nav: { flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, navTitle: { fontWeight: "800", fontSize: 16 }, saveTop: { fontSize: 13, fontWeight: "800" }, title: { fontSize: 28, fontWeight: "800", marginTop: 7 }, subtitle: { fontSize: 13, lineHeight: 19 }, label: { fontSize: 10, fontWeight: "800", letterSpacing: 1, marginTop: 8 }, nameInput: { height: 51, borderRadius: 14, paddingHorizontal: 14, fontSize: 15, fontWeight: "700" }, section: { fontSize: 19, fontWeight: "800", marginTop: 8 }, exercise: { minHeight: 62, borderRadius: 16, borderWidth: 1, padding: 11, flexDirection: "row", alignItems: "center", gap: 10 }, check: { width: 27, height: 27, borderRadius: 9, alignItems: "center", justifyContent: "center" }, exerciseName: { fontSize: 13, fontWeight: "800" }, exerciseGroup: { fontSize: 11, marginTop: 4 }, fields: { flexDirection: "row", gap: 8 }, fieldLabel: { fontSize: 10, fontWeight: "700", marginBottom: 5 }, field: { height: 48, borderRadius: 12, borderWidth: 1, paddingHorizontal: 10, fontSize: 15, fontWeight: "800" }, button: { minHeight: 54, borderRadius: 16, alignItems: "center", justifyContent: "center", marginTop: 8 }, buttonText: { color: "#101412", fontSize: 15, fontWeight: "800" } });
