import { useState } from "react";
import { Pressable, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";
import { router } from "expo-router";
import { ScreenContainer } from "@/components/screen-container";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { useColors } from "@/hooks/use-colors";
import { type OneRepMaxFormula } from "@/lib/workout-data";
import { useWorkoutStore } from "@/lib/workout-store";

const formulas: { id: OneRepMaxFormula; title: string; formula: string; description: string }[] = [{ id: "epley", title: "Эпли", formula: "Вес × (1 + повторы / 30)", description: "Универсальная оценка для большинства рабочих подходов." }, { id: "brzycki", title: "Бржицки", formula: "Вес × 36 / (37 − повторы)", description: "Удобна для небольшого числа повторений." }];

export default function SettingsScreen() {
  const colors = useColors();
  const store = useWorkoutStore();
  const { oneRmFormula, setOneRmFormula, plateStepKg, setPlateStepKg, bodyWeightKg, bodyweightVolumePercent, setBodyweightVolumeSettings } = store;
  const [bodyWeight, setBodyWeight] = useState(String(bodyWeightKg));
  const [bodyPercent, setBodyPercent] = useState(String(bodyweightVolumePercent));
  const links = [{ title: "Профиль штанги и блинов", subtitle: "Вес грифа и доступные номиналы", route: "/barbell" as const }, { title: "Рекомендации следующей тренировки", subtitle: "Рабочий вес с объяснением расчёта", route: "/recommendations" as const }, { title: "График прогресса 1RM", subtitle: "Изменение силового показателя по датам", route: "/progress" as const }, { title: "Экспорт истории CSV", subtitle: "Файл для Excel и других таблиц", route: "/export" as const }, { title: "Месячный PDF-отчёт", subtitle: "Красивые итоги и таблица подходов", route: "/report" as const }];
  return <ScreenContainer edges={["top", "left", "right", "bottom"]} className="px-5" containerClassName="bg-background"><ScrollView contentContainerStyle={styles.content}><View style={styles.header}><Pressable onPress={() => router.back()}><IconSymbol name="chevron.left" size={27} color={colors.foreground} /></Pressable><Text style={[styles.headerTitle, { color: colors.foreground }]}>Настройки</Text><View style={{ width: 27 }} /></View><Text style={[styles.eyebrow, { color: colors.primary }]}>РАСЧЁТ СИЛЫ</Text><Text style={[styles.title, { color: colors.foreground }]}>Формула 1RM</Text><Text style={[styles.subtitle, { color: colors.muted }]}>Выбор применяется к зонам, истории и личным рекордам.</Text><View style={styles.options}>{formulas.map((option) => { const selected = option.id === oneRmFormula; return <Pressable key={option.id} onPress={() => setOneRmFormula(option.id)} style={[styles.option, { backgroundColor: colors.surface, borderColor: selected ? colors.primary : colors.border }]}><View style={[styles.radio, { borderColor: selected ? colors.primary : colors.muted }]}>{selected && <View style={[styles.radioDot, { backgroundColor: colors.primary }]} />}</View><View style={{ flex: 1 }}><Text style={[styles.optionTitle, { color: colors.foreground }]}>{option.title}</Text><Text style={[styles.optionFormula, { color: colors.primary }]}>{option.formula}</Text><Text style={[styles.optionDescription, { color: colors.muted }]}>{option.description}</Text></View></Pressable>; })}</View><Text style={[styles.sectionTitle, { color: colors.foreground }]}>Шаг веса</Text><View style={styles.stepRow}>{[1.25, 2.5, 5].map((step) => <Pressable key={step} onPress={() => setPlateStepKg(step)} style={[styles.step, { backgroundColor: plateStepKg === step ? colors.primary : colors.surface, borderColor: plateStepKg === step ? colors.primary : colors.border }]}><Text style={{ color: plateStepKg === step ? "#101412" : colors.foreground, fontWeight: "800" }}>{step} кг</Text></Pressable>)}</View><Text style={[styles.sectionTitle, { color: colors.foreground }]}>Упражнения с весом тела</Text><Text style={[styles.subtitle, { color: colors.muted }]}>Для тоннажа приложение использует заданную долю массы тела: например, приседания или отжимания.</Text><View style={styles.bodyFields}><View style={{ flex: 1 }}><Text style={[styles.fieldLabel, { color: colors.muted }]}>Вес тела, кг</Text><TextInput value={bodyWeight} onChangeText={setBodyWeight} keyboardType="decimal-pad" onEndEditing={() => setBodyweightVolumeSettings(Number(bodyWeight) || bodyWeightKg, Number(bodyPercent) || bodyweightVolumePercent)} style={[styles.field, { color: colors.foreground, borderColor: colors.border, backgroundColor: colors.surface }]} /></View><View style={{ flex: 1 }}><Text style={[styles.fieldLabel, { color: colors.muted }]}>Учитывать, %</Text><TextInput value={bodyPercent} onChangeText={setBodyPercent} keyboardType="number-pad" onEndEditing={() => setBodyweightVolumeSettings(Number(bodyWeight) || bodyWeightKg, Number(bodyPercent) || bodyweightVolumePercent)} style={[styles.field, { color: colors.foreground, borderColor: colors.border, backgroundColor: colors.surface }]} /></View></View><Text style={[styles.sectionTitle, { color: colors.foreground }]}>Данные и инструменты</Text>{links.map((link) => <Pressable key={link.route} onPress={() => router.push(link.route)} style={[styles.link, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={{ flex: 1 }}><Text style={[styles.linkTitle, { color: colors.foreground }]}>{link.title}</Text><Text style={[styles.linkSubtitle, { color: colors.muted }]}>{link.subtitle}</Text></View><IconSymbol name="chevron.right" size={20} color={colors.muted} /></Pressable>)}<View style={[styles.note, { backgroundColor: colors.surface }]}><IconSymbol name="checkmark.circle" size={20} color={colors.primary} /><Text style={[styles.noteText, { color: colors.muted }]}>Настройки и личные рекорды сохраняются локально и резервируются в профиль при подключении к сети.</Text></View></ScrollView></ScreenContainer>;
}
const styles = StyleSheet.create({ content: { paddingTop: 16, paddingBottom: 34, gap: 12 }, header: { minHeight: 42, flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, headerTitle: { fontSize: 16, fontWeight: "800" }, eyebrow: { fontSize: 11, fontWeight: "900", letterSpacing: 1.2, marginTop: 10 }, title: { fontSize: 28, fontWeight: "800", marginTop: 5 }, subtitle: { fontSize: 13, lineHeight: 20, marginTop: 4 }, options: { gap: 10, marginTop: 10 }, option: { borderWidth: 1, borderRadius: 18, padding: 14, flexDirection: "row", gap: 12 }, radio: { width: 22, height: 22, borderRadius: 11, borderWidth: 2, alignItems: "center", justifyContent: "center", marginTop: 2 }, radioDot: { width: 11, height: 11, borderRadius: 6 }, optionTitle: { fontSize: 16, fontWeight: "800" }, optionFormula: { fontSize: 12, fontWeight: "800", marginTop: 5 }, optionDescription: { fontSize: 12, lineHeight: 18, marginTop: 7 }, sectionTitle: { fontSize: 18, fontWeight: "800", marginTop: 9 }, stepRow: { flexDirection: "row", gap: 9 }, step: { flex: 1, minHeight: 44, borderRadius: 13, borderWidth: 1, alignItems: "center", justifyContent: "center" }, bodyFields: { flexDirection: "row", gap: 9 }, fieldLabel: { fontSize: 10, fontWeight: "800", marginBottom: 5 }, field: { height: 48, borderWidth: 1, borderRadius: 13, paddingHorizontal: 12, fontSize: 15, fontWeight: "800" }, link: { minHeight: 67, borderRadius: 16, borderWidth: 1, padding: 13, flexDirection: "row", alignItems: "center", gap: 9 }, linkTitle: { fontSize: 14, fontWeight: "800" }, linkSubtitle: { fontSize: 11, marginTop: 4 }, note: { borderRadius: 16, padding: 14, flexDirection: "row", gap: 10, marginTop: 6 }, noteText: { flex: 1, fontSize: 12, lineHeight: 18 } });
